const wrapperExplorer = require('./wrapper-explorer');

console.log(`in wrapper demo.js file`);

console.log('__filename in WD', __filename);
console.log('__dirname in WD', __dirname);

wrapperExplorer.greet('Kaze');